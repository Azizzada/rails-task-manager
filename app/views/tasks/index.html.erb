<h1>tasks</h1>
<ul>
<% @tasks.each do |task| %>
<li><%=task.completed%><%=link_to task.title, task_path(task)%><%= link_to "🗑🚮",
task_path(task),
data: {turbo_method: :delete, turbo_confirm: "are you sure?"}  %></li>
<% end %>
</ul>

<%= link_to"create new task", new_task_path%>
